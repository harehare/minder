[graph]
all-features = false
no-default-features = false
targets = []

[output]
feature-depth = 1

[advisories]
ignore = [
  # Transitive via mq-lang (upstream https://github.com/harehare/mq), not
  # code paths minder's usage (features = ["std", "sync", "file-io"])
  # exercises. Revisit when mq-lang bumps bincode/quick-xml upstream.
  "RUSTSEC-2025-0141", # bincode unmaintained
  "RUSTSEC-2026-0194", # quick-xml quadratic runtime on duplicate attrs
  "RUSTSEC-2026-0195", # quick-xml unbounded namespace-declaration allocation
]

[licenses]
allow = [
  "Apache-2.0 WITH LLVM-exception",
  "Apache-2.0",
  "BSD-3-Clause",
  "BSL-1.0",
  "CDLA-Permissive-2.0",
  "ISC",
  "MIT",
  "MPL-2.0",
  "NCSA",
  "Unicode-3.0",
  "Zlib",
  "MIT-0",
  "OpenSSL",
]
confidence-threshold = 0.8
exceptions = []

[licenses.private]
ignore = false
registries = []

[bans]
allow = []
deny = []
external-default-features = "allow"
highlight = "all"
multiple-versions = "warn"
skip = []
skip-tree = []
wildcards = "allow"
workspace-default-features = "allow"

[sources]
allow-git = []
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
unknown-git = "warn"
unknown-registry = "warn"

[sources.allow-org]
bitbucket = []
github = []
gitlab = []
