[workspace]

[package]
name = "todo_plugin"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "todo_plugin"
path = "src/main.rs"

[profile.release]
panic = "abort"
